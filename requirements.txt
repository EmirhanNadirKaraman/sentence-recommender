psycopg2-binary>=2.9
alembic>=1.13                  # this project's own Postgres schema
sqlalchemy>=2.0                # alembic needs it; nothing else here does
spacy>=3.8
spacy-lookups-data>=1.0        # German lemma table; see corpus/analyzer.py
requests>=2.31                 # the local model client

# The German model is not on PyPI.  The *medium* model, not the small one:
# the vendored matcher comes from language-app, which requires `md` or larger
# for lemmatisation, and under `sm` it returns `schreie` for `schreien` and
# `samen` for `gesamt`.  See matcher/phrase_finder.py.
#   python -m spacy download de_core_news_md

pulp>=3.0                      # `cover` only; the wheel carries its own CBC

# `export-deck` only — the teaching order as a document and a slideshow.
reportlab>=4.0
python-pptx>=1.0

# `speak-deck` only — reads the deck aloud with a model on this machine.
# The voices are downloaded on first use into data/voices.  The transformers
# backend is for weights of your own and needs torch as well; neither is
# required to run anything else here.
piper-tts>=1.8

# `export-anki` only — the deck as .apkg files with the audio inside.
genanki>=0.13

# `detect-language` only — which language a stored sentence is in. `lingua`
# rather than `langdetect`: measured over 600 real sentences, langdetect
# flagged seven as not-German of which five plainly were (`Papa, wann können
# wir Samuel abholen?`), because it is a port of a library built for
# documents and this corpus is single short lines. lingua flagged two and
# both were English.
lingua-language-detector>=2.0

# `mcp-serve` only — the roadmap and the reviews as MCP tools over stdio, for
# Claude Code or any MCP client to launch. The bare package: its `[cli]`
# extra is `mcp dev`, which runs a server file under its own interpreter,
# and this one has to run under .venv's (see the note on spaCy above).
mcp>=2.2

# Experiment 04's `laya` arm only: the open-weight System One model, run on
# this machine against the same rows Jev was scored on. Measured at chance
# (AUC 0.46–0.51 across its three checkpoints); kept so the arm reruns.
# Its checkpoints live in ~/.cache/huggingface/hub/models--convaiinnovations--laya
# (2.2 GB) and can be deleted with the package.
laya>=0.3.4

# Nothing in this repository imports it: `config.py` has its own `load_dotenv`,
# twelve lines that read KEY=VALUE without overriding a real environment
# variable. It is here for the scraper next door. `ingest/video.py` defers an
# `import pipeline`, which resolves to
# `../language-app/subtitle-scraper/pipeline.py`, and that file imports the
# real package.
#
# Deferred is why this went unnoticed: the import is inside a property, so a
# checkout can run the server, the walks and the whole test suite without ever
# reaching it, and only `add-video` and `add-channel` fail -- with a
# ModuleNotFoundError naming a package this project appears not to use.
python-dotenv>=1.0
