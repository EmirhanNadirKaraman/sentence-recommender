# Database structure

Generated by `python main.py schema-doc` — regenerate rather than
hand-edit.

_As of 2026-09-20._

Two stores, and the split is deliberate. Postgres holds the catalogue
— what German exists. SQLite holds what has been made of it, and what
the reader has decided.

## `data/state.sqlite3` — what the reader does (33 tables)

What the reader has done, and nothing else — the corpus itself moved to
Postgres. Roadmaps and scores are derived and rebuildable. The judgements are
not: `known_units`, `checked_units`, `snoozed_units`, `hidden_sentences`, `corrected_sentences`, `video_attempts` hold decisions
nothing can regenerate. The file is gitignored either way.


### `audio_piece` — 40,793 rows

| column | type | null |
|---|---|---|
| `id` | TEXT | yes |
| `text` | TEXT | no |
| `voice` | TEXT | no |
| `slow` | INTEGER | no |
| `role` | TEXT | no |
| `path` | TEXT | no |
| `seconds` | REAL | no |
| `made_at` | TEXT | no |

- primary key: `id`
- index `ix_piece_role` on (role)

### `card_audio` — 7,494 rows

| column | type | null |
|---|---|---|
| `stem` | TEXT | yes |
| `recipe` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `stem`

### `cards` — 84,607 rows

| column | type | null |
|---|---|---|
| `card_id` | INTEGER | yes |
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `due_date` | TEXT | no |
| `interval_days` | REAL | no |
| `ease_factor` | REAL | no |
| `repetitions` | INTEGER | no |
| `last_review` | TEXT | yes |

- primary key: `card_id`
- index `ix_cards_due` on (due_date)

### `channel_blacklist` — 0 rows

| column | type | null |
|---|---|---|
| `channel_id` | TEXT | yes |
| `decided` | TEXT | no |

- primary key: `channel_id`

### `channel_blacklist_version` — 1 rows

| column | type | null |
|---|---|---|
| `id` | INTEGER | yes |
| `version` | INTEGER | no |

- primary key: `id`

### `channel_taste` — 2 rows

| column | type | null |
|---|---|---|
| `channel_id` | TEXT | yes |
| `taste` | TEXT | no |
| `decided` | TEXT | no |

- primary key: `channel_id`

### `channel_taste_version` — 1 rows

| column | type | null |
|---|---|---|
| `id` | INTEGER | yes |
| `version` | INTEGER | no |

- primary key: `id`

### `checked_units` — 81 rows

| column | type | null |
|---|---|---|
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `checked` | TEXT | no |

- primary key: `kind`, `key`

### `corrected_sentences` — 0 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | yes |
| `corrected` | TEXT | no |

- primary key: `text`

### `goal_search` — 37 rows

| column | type | null |
|---|---|---|
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `tries` | INTEGER | no |
| `last` | TEXT | no |

- primary key: `kind`, `key`

### `hidden_sentences` — 2 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | yes |
| `hidden` | TEXT | no |

- primary key: `text`

### `known_units` — 105 rows

| column | type | null |
|---|---|---|
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `marked` | TEXT | no |

- primary key: `kind`, `key`

### `known_version` — 1 rows

| column | type | null |
|---|---|---|
| `id` | INTEGER | yes |
| `version` | INTEGER | no |

- primary key: `id`

### `roadmap` — 15,434 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | no |
| `position` | INTEGER | no |
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `sentence` | TEXT | no |
| `translation` | TEXT | yes |
| `origin` | TEXT | no |
| `surface` | TEXT | yes |
| `gain` | INTEGER | no |
| `score` | REAL | no |
| `now_readable` | INTEGER | no |
| `readable` | INTEGER | no |
| `occurrences` | INTEGER | no |
| `beside_kind` | TEXT | yes |
| `beside_key` | TEXT | yes |

- primary key: `source`, `position`

### `roadmap_example` — 283,570 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | no |
| `position` | INTEGER | no |
| `n` | INTEGER | no |
| `text` | TEXT | no |
| `translation` | TEXT | yes |
| `origin` | TEXT | no |
| `surface` | TEXT | yes |
| `units` | TEXT | no |
| `video_id` | TEXT | yes |
| `start_time` | REAL | yes |
| `end_time` | REAL | yes |

- primary key: `source`, `position`, `n`

### `roadmap_meta` — 5 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | yes |
| `stamp` | TEXT | no |
| `made_at` | TEXT | no |
| `total` | INTEGER | no |

- primary key: `source`

### `sentence_english` — 54,401 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | yes |
| `english` | TEXT | no |
| `model` | TEXT | no |
| `made_at` | TEXT | no |
| `version` | INTEGER | no |
| `source` | TEXT | no |

- primary key: `text`

### `sentence_fix` — 12,706 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | yes |
| `fixed` | TEXT | no |
| `model` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `text`

### `sentence_units_override` — 0 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | no |
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `surface` | TEXT | yes |

- primary key: `text`, `kind`, `key`

### `sentence_vector` — 61,978 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | yes |
| `vector` | BLOB | no |
| `dims` | INTEGER | no |
| `model` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `text`

### `sentence_verdict` — 21,830 rows

| column | type | null |
|---|---|---|
| `text` | TEXT | no |
| `verdict` | REAL | no |
| `source` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `text`, `source`

### `snoozed_units` — 1 rows

| column | type | null |
|---|---|---|
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `wake_tick` | INTEGER | no |
| `times` | INTEGER | no |
| `snoozed` | TEXT | no |

- primary key: `kind`, `key`
- index `ix_snoozed_wake` on (wake_tick)

### `study_clock` — 1 rows

| column | type | null |
|---|---|---|
| `id` | INTEGER | yes |
| `tick` | INTEGER | no |

- primary key: `id`

### `unit_sense` — 16,505 rows

| column | type | null |
|---|---|---|
| `kind` | TEXT | no |
| `key` | TEXT | no |
| `text` | TEXT | no |
| `means` | TEXT | no |
| `model` | TEXT | no |
| `made_at` | TEXT | no |
| `version` | INTEGER | no |

- primary key: `kind`, `key`, `text`

### `video_attempts` — 2,832 rows

| column | type | null |
|---|---|---|
| `video_id` | TEXT | yes |
| `outcome` | TEXT | no |
| `detail` | TEXT | yes |
| `tried` | TEXT | no |
| `tries` | INTEGER | no |

- primary key: `video_id`

### `video_cover` — 40 rows

| column | type | null |
|---|---|---|
| `list` | TEXT | no |
| `build` | TEXT | no |
| `position` | INTEGER | no |
| `video_id` | TEXT | no |
| `minutes` | REAL | yes |

- primary key: `list`, `build`, `position`

### `video_cover_meta` — 1 rows

| column | type | null |
|---|---|---|
| `list` | TEXT | no |
| `build` | TEXT | no |
| `stamp` | TEXT | no |
| `made_at` | TEXT | no |
| `depth` | INTEGER | no |
| `minutes` | REAL | no |
| `covered` | INTEGER | no |
| `short` | INTEGER | no |
| `unreachable` | INTEGER | no |

- primary key: `list`, `build`

### `video_roadmap` — 1,878 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | no |
| `position` | INTEGER | no |
| `video_id` | TEXT | no |
| `teaches` | INTEGER | no |
| `minutes` | REAL | yes |
| `lines` | INTEGER | no |
| `readable` | INTEGER | no |

- primary key: `source`, `position`

### `video_roadmap_meta` — 1 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | yes |
| `stamp` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `source`

### `video_score` — 4,764 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | no |
| `video_id` | TEXT | no |
| `title` | TEXT | yes |
| `lines` | INTEGER | no |
| `minutes` | REAL | yes |
| `comprehension` | REAL | no |
| `teachable` | INTEGER | no |
| `teaches` | INTEGER | no |
| `watch` | REAL | no |
| `next_words` | TEXT | no |

- primary key: `source`, `video_id`

### `video_score_meta` — 4 rows

| column | type | null |
|---|---|---|
| `source` | TEXT | yes |
| `stamp` | TEXT | no |
| `made_at` | TEXT | no |

- primary key: `source`

### `word_list` — 2,052 rows

| column | type | null |
|---|---|---|
| `name` | TEXT | no |
| `position` | INTEGER | no |
| `entry` | TEXT | no |

- primary key: `name`, `position`

### `word_list_meta` — 3 rows

| column | type | null |
|---|---|---|
| `name` | TEXT | yes |
| `saved` | TEXT | no |
| `note` | TEXT | no |

- primary key: `name`

## Postgres `sentence_recommender` — the catalogue (12 tables)

This project's own database, under `alembic` at revision `4d7b9a2c1e33`. Every
read and every write goes here; `DB_NAME` names it.

It began as a copy of language-app's `german_vocabulary`, made by
`sync-catalogue --from german_vocabulary`. That was a migration, not a
link: ingestion writes here now, so re-copying would destroy work, and
the command refuses when it would.

Tables marked *write-only* are filled by language-app's scraper during
ingestion and read by nothing in this project.


### `channel` — 520 rows

| column | type | null | default |
|---|---|---|---|
| `id` | integer | no |  |
| `youtube_channel_id` | text | no |  |
| `channel_name` | text | no | `''` |
| `language` | text | yes |  |
| `active` | boolean | no | `true` |

- primary key: `PRIMARY KEY (id)`
- unique: `UNIQUE (youtube_channel_id)`
- index `ix_channel_language_active` — `btree (language, active)`

### `video` — 2,887 rows

| column | type | null | default |
|---|---|---|---|
| `video_id` | text | no |  |
| `title` | text | no |  |
| `thumbnail_url` | text | no |  |
| `duration` | double precision | no |  |
| `language` | text | no |  |
| `dialect` | text | no |  |
| `category` | text | no | `'other'` |
| `channel_id` | integer | yes |  |
| `transcript_source` | text | no | `'auto'` |

- foreign key: `FOREIGN KEY (channel_id) REFERENCES channel(id)`
- primary key: `PRIMARY KEY (video_id)`
- index `ix_video_channel` — `btree (channel_id)`
- index `ix_video_language` — `btree (language, video_id)`

### `sentence` — 643,892 rows

| column | type | null | default |
|---|---|---|---|
| `sentence_id` | integer | no |  |
| `video_id` | text | no |  |
| `start_time` | double precision | no |  |
| `duration` | double precision | no |  |
| `content` | text | no |  |
| `tokens` | ARRAY | no |  |
| `token_ids` | ARRAY | yes |  |

- foreign key: `FOREIGN KEY (video_id) REFERENCES video(video_id) ON DELETE CASCADE`
- primary key: `PRIMARY KEY (sentence_id)`
- index `ix_sentence_video_time` — `btree (video_id, start_time, sentence_id)`

### `word_table` — 214,183 rows

| column | type | null | default |
|---|---|---|---|
| `word_id` | integer | no |  |
| `word` | text | no |  |
| `language` | text | no |  |
| `pos` | text | no |  |
| `tag` | text | no |  |
| `lemma` | text | no |  |
| `frequency` | integer | no | `0` |
| `word_norm` | text | yes |  |

- primary key: `PRIMARY KEY (word_id)`
- unique: `UNIQUE (word, language, pos)`
- index `ix_word_language_lower_lemma` — `btree (language, lower(lemma))`
- index `ix_word_language_tag` — `btree (language, tag) INCLUDE (lemma, frequency, word)`
- index `ix_word_language_word_norm` — `btree (language, word_norm)`

### `phrase_table` — 2,829 rows

| column | type | null | default |
|---|---|---|---|
| `phrase_id` | integer | no |  |
| `canonical` | text | no |  |
| `surface_form` | text | no |  |
| `phrase_type` | text | no | `'verb_pattern'` |
| `language` | text | no | `'de'` |

- primary key: `PRIMARY KEY (phrase_id)`
- unique: `UNIQUE (canonical, language)`
- index `ix_phrase_language_canonical` — `btree (language, canonical)`

### `grammar_rule` — 89 rows

| column | type | null | default |
|---|---|---|---|
| `rule_id` | integer | no |  |
| `rule` | text | no |  |
| `language` | text | no |  |

- primary key: `PRIMARY KEY (rule_id)`
- unique: `UNIQUE (rule, language)`
- index `ix_grammar_language` — `btree (language)`

### `phrase_blueprint` — 107,236 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `blueprint_id` | integer | no |  |
| `lookup_key` | text | no |  |
| `blueprint` | text | no |  |
| `lookup_key_norm` | text | yes |  |

- primary key: `PRIMARY KEY (blueprint_id)`
- unique: `UNIQUE (lookup_key)`

### `sentence_to_grammar_rule` — 6,300,342 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `sentence_id` | integer | no |  |
| `rule_id` | integer | no |  |

- foreign key: `FOREIGN KEY (rule_id) REFERENCES grammar_rule(rule_id) ON DELETE CASCADE`
- foreign key: `FOREIGN KEY (sentence_id) REFERENCES sentence(sentence_id) ON DELETE CASCADE`
- primary key: `PRIMARY KEY (sentence_id, rule_id)`

### `word_to_sentence` — 5,150,685 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `word_id` | integer | no |  |
| `sentence_id` | integer | no |  |

- foreign key: `FOREIGN KEY (sentence_id) REFERENCES sentence(sentence_id) ON DELETE CASCADE`
- foreign key: `FOREIGN KEY (word_id) REFERENCES word_table(word_id) ON DELETE CASCADE`
- primary key: `PRIMARY KEY (word_id, sentence_id)`
- index `wts_sentence_id_index` — `btree (sentence_id)`

### `sentence_to_phrase` — 4,318,176 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `id` | integer | no |  |
| `sentence_id` | integer | no |  |
| `blueprint_id` | integer | yes |  |
| `surface_form` | text | no |  |
| `logic` | text | no |  |
| `match_type` | text | no |  |
| `indices` | ARRAY | no |  |

- foreign key: `FOREIGN KEY (blueprint_id) REFERENCES phrase_blueprint(blueprint_id) ON DELETE SET NULL`
- foreign key: `FOREIGN KEY (sentence_id) REFERENCES sentence(sentence_id) ON DELETE CASCADE`
- primary key: `PRIMARY KEY (id)`
- index `stp_sentence_id_idx` — `btree (sentence_id)`

### `lemma_override` — 2 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `id` | integer | no |  |
| `language` | text | no |  |
| `observed_lemma` | text | no |  |
| `corrected_lemma` | text | no |  |
| `surface_form` | text | yes |  |
| `pos` | text | yes |  |
| `source` | text | no | `'manual'` |
| `status` | text | no | `'active'` |
| `confidence` | real | yes |  |
| `created_at` | timestamp with time zone | no | `now()` |
| `updated_at` | timestamp with time zone | no | `now()` |

- check: `CHECK ((source = ANY (ARRAY['manual'::text, 'llm'::text, 'admin'::text, 'user_flag_reviewed'::text])))`
- check: `CHECK ((status = ANY (ARRAY['active'::text, 'inactive'::text])))`
- primary key: `PRIMARY KEY (id)`
- unique: `UNIQUE (language, observed_lemma)`
- index `ix_lemma_override_lang_status` — `btree (language, status)`

### `video_blacklist` — 357 rows *(write-only)*

| column | type | null | default |
|---|---|---|---|
| `video_id` | text | no |  |

- primary key: `PRIMARY KEY (video_id)`

## Postgres `sentence_recommender` — the analysed corpus (3 tables + 1 view)

What the matcher made of the catalogue: one row per sentence worth
reading, and one per learning unit in it. `build` names both the source
and how it was assembled, so two ways of cutting the same subtitles can
coexist; `build_meta` records which rules produced each.

These were SQLite until the i+1 subtraction — 3.48M set operations per
load — was measured at 2.18s in SQL against 10.04s in Python.


### `corpus_sentence` — 450,547 rows

| column | type | null | default |
|---|---|---|---|
| `id` | integer | no |  |
| `build` | text | no |  |
| `origin` | text | no |  |
| `text` | text | no |  |
| `translation` | text | yes |  |
| `raw_text` | text | yes |  |
| `source_ids` | text | no | `''` |
| `video_id` | text | yes |  |
| `start_time` | double precision | yes |  |
| `end_time` | double precision | yes |  |
| `teachable` | boolean | no | `true` |
| `language` | text | yes |  |

- primary key: `PRIMARY KEY (id)`
- index `ix_corpus_sentence_build` — `btree (build, teachable, id)`
- index `ix_corpus_sentence_text` — `hash (text)`
- index `ix_corpus_sentence_video` — `btree (build, video_id, start_time)`

### `corpus_unit` — 3,313,771 rows

| column | type | null | default |
|---|---|---|---|
| `sentence_id` | integer | no |  |
| `kind` | text | no |  |
| `key` | text | no |  |
| `surface` | text | yes |  |

- foreign key: `FOREIGN KEY (sentence_id) REFERENCES corpus_sentence(id) ON DELETE CASCADE`
- primary key: `PRIMARY KEY (sentence_id, kind, key)`
- index `ix_corpus_unit_key` — `btree (kind, key, sentence_id)`

### `build_meta` — 3 rows

| column | type | null | default |
|---|---|---|---|
| `build` | text | no |  |
| `fingerprint` | text | no |  |
| `made_at` | text | no |  |
| `packages` | text | no | `''` |

- primary key: `PRIMARY KEY (build)`

### `corpus_unit_count` — 94,120 rows *(materialized view)*

Refreshed CONCURRENTLY when a build is written. 59ms to read,
against 197ms to group the unit rows live.

```sql
SELECT s.build,
    u.kind,
    u.key,
    (count(*))::integer AS said
   FROM (corpus_unit u
     JOIN corpus_sentence s ON ((s.id = u.sentence_id)))
  GROUP BY s.build, u.kind, u.key;
```
