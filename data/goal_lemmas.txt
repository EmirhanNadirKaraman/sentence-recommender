# Goal entries the parser mis-lemmatises when it sees them alone.
#
# `GoalList` resolves each word in the study list into the lemma space the
# corpus is analysed in, by running it through spaCy. A bare word carries no
# sentence around it, which is where the parser is weakest: it returns
# "schreie" for "schreien" and "samen" for "gesamt" — confidently, so
# `lemma_overrides.txt` never fires (that one only helps where the parser
# gives up and echoes the surface back).
#
# The result is a goal for a word that does not exist, which no sentence can
# ever satisfy. Three of these showed up on the Blocked page; the rest
# quietly became goals for a different word, which is worse, because nothing
# reports it.
#
# One line per correction: the entry as the study list writes it, then the
# unit to use instead. Listed rather than inferred — the parser's other
# rewrites here are corrections ("im" to "in", "geboren" to "gebären"), and
# no rule separates those from the damage.

schreien	schreien
rauschen	rauschen
weinen	weinen
klappen	klappen
reisen	reisen
hingehen	hingehen
aufwachsen	aufwachsen
vorherrschen	vorherrschen
bisschen	bisschen
besonder	besonder
inner	inner
euer	euer
letzt	letzt
gesamt	gesamt
lieb	lieb
bewusst	bewusst
einverstanden	einverstanden
jdn. zu etw. einladen	einladen
ander	anderer
# `gucken, kucken` is registered as a pattern canonical, and no mechanism
# emits it. The case-frame path produces a pattern only for a verb with
# objects — `Ich gucke mir das an` yields the angucken frame — and the
# variant path only rewrites a lemma that differs from the canonical's head,
# which `gucken` does not. So the goal sat unreachable while the corpus said
# `gucken` 100+ times. Corrected to the lemma the corpus actually emits.
gucken, kucken	gucken
# `die Unterlagen` is written as article-plus-plural and the analyser emits
# the bare singular lemma, so the goal matched nothing while the corpus said
# `unterlage` 104 times. Same shape as `gucken, kucken` above: a goal in a
# form no mechanism produces.
die Unterlagen	unterlage
# Nominalised adjectives and a plural, all written the way a dictionary
# writes them and all emitted by the analyser as a bare lemma. Each was on
# the blocked list as "never said at all" while the corpus said it dozens of
# times — 61, 51, 30, 18 and 19 respectively. Found by comparing the raw
# sentence text against the unit counts, which disagree whenever a goal is
# written in a form nothing produces.
der Abgeordnete, die Abgeordnete	abgeordneter
der Vorsitzende, die Vorsitzende	vorsitzend
der Beamte, die Beamte	beamter
der Angestellte, die Angestellte	angestellter
die Aufwendungen	aufwendung
