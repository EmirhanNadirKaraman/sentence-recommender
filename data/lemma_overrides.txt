# Verb forms neither the parser nor spaCy's German lookup table resolves.
#
# One line per correction: the written form, then the lemma it belongs to.
# Consulted ONLY for a token the parser tagged as a verb and failed to
# lemmatise, so an entry here cannot affect the same spelling used as another
# part of speech — "weiß" stays "weiß" when it is the colour.
#
# Add a line whenever an inflected form shows up in the roadmap that should
# have been folded into its infinitive.

muss    müssen
musst   müssen
soll    sollen

# German drops the -e of the first person and the imperative as soon as anyone
# is speaking rather than writing: habe/sage/gehe/mache/höre/lasse become hab,
# sag, geh, mach, hör, lass. spaCy's lemma table is built from written
# wordlists that carry the full forms and none of the clipped ones, and its
# lemmatiser leaves what it cannot place alone — so every one of these came
# through as a unit of its own, sitting beside the infinitive the reader
# already knows.
#
# 2,454 subtitle sentences hold one. 255 of them have no other unknown at all,
# so they are unreadable for no reason but this.

hab     haben
lass    lassen
sag     sagen
hör     hören
mach    machen
geh     gehen

# Not a clipped form: a plain third-person present, and simply missing from
# the table. The parser hands it back unchanged like the rest.
lässt   lassen

# A second sweep, over every identity lemma rather than the most frequent few.
# Three shapes the first pass could not see:
#
#   contractions      the verb and a swallowed "es", written as one token
#   suffix swaps      -t replaced rather than -en appended, so guessing
#                     "surface + en" produced lassten and müssten and scored
#                     them as having no infinitive at all
#   the quiet ones    bring, at 59 uses, sat below the frequency cut
#
# 590 sentences hold one; 56 have no other unknown. Each target was already
# known, so folding removes the unit rather than renaming it.

versuch  versuchen
stell    stellen
bring    bringen
lasst    lassen
müsst    müssen

# The verb with a swallowed "es". The pronoun is a function word either way,
# so nothing is lost by reading the token as the verb it carries.
geht's   gehen
gibt's   geben

# The parser does not only fail cleanly, it also invents — and an invented
# lemma used to slip past the correction written for it, because the entry was
# only consulted when the lemma came back identical to the surface. `muss` came
# back as `mussn` and `mussen`, `hab` as `habn`, and each became a unit of its
# own. A written line is trusted over the parser now, so these fire.
#
# Every one of these was found in the corpus as an invented unit: the count is
# how many rows it had taken.

weißt     wissen
kannst    können
wusste    wissen
gewusst   wissen
kommst    kommen
brauchst  brauchen
siehst    sehen
sieh      sehen
gehst     gehen
musste    müssen
mussten   müssen

# The tail of the same sweep. `mussen` is a typo in the subtitle itself, not a
# parser slip — it is written that way on screen — but it is tagged as a verb,
# so the correction reaches it.
#
# Two more rows are left alone on purpose: `Sieh` comes back tagged ADJD and
# `gehst` ADV, both in damaged lines, and the verb guard refuses them. That
# guard is what keeps the adjective `weißen` from being folded into `wissen`,
# and three rows in 1.18 million is not a reason to loosen it.

wussten   wissen
mussen    müssen

# Forms the invention check now hands back as themselves. Each was previously
# some string the parser made up — `hältst` came back as `hälten`, `sprichst`
# as `sprichen` — so the entry could never fire; refusing the invention is
# what makes them reachable.

hältst    halten
sprichst  sprechen
nehm      nehmen
schau     schauen
findest   finden
verstehst verstehen
bekommst  bekommen
bringst   bringen
merkt     merken
wusstest  wissen
hättest   haben
